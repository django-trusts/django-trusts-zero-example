# Python >=3.12, Django 6.1.x — paired Zero + schema-neutral core.
# Pin the reviewed #142 pair: Core Stage A (PR #144) and Zero Z-convert
# (PR #26). Unpinned django-trusts resolves to PyPI 0.10.x. Do not pin
# the pre-split core commit 8916a76.
Django>=6.1,<6.2
django-trusts @ git+https://github.com/django-trusts/django-trusts.git@710b3ea26778ff069d1f5329adc9f2f481a1ea92
django-trusts-zero @ git+https://github.com/django-trusts/django-trusts-zero.git@bceb0241b482fdc4f31dd72c7600c52eeb4e6cff
gunicorn>=23.0.0
# Pure-Python driver: mysqlclient has no Linux wheels and needs client headers
# the stock Herokuish Python buildpack does not provide.
# 1.2.0+ reports MySQLdb.version_info >= (2, 2, 1), which Django 6.1 requires.
# 1.1.1 reports 1.4.6 and raises ImproperlyConfigured at backend import.
PyMySQL[rsa]>=1.2.0
dj-database-url>=2.3.0
whitenoise>=6.9.0
